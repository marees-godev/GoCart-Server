package resolvers

// This file will be automatically regenerated based on the schema, any resolver implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.49

import (
	"context"

	storepb "github.com/marees-godev/GoCart-Server/contracts/protobuf/store"
	maps "github.com/marees-godev/GoCart-Server/gateway/api-gateway/internal/graphql/mappers"
	"github.com/marees-godev/GoCart-Server/gateway/api-gateway/internal/graphql/model"
	"github.com/marees-godev/GoCart-Server/pkg/auth"
	appErrors "github.com/marees-godev/GoCart-Server/pkg/errors"
	"github.com/marees-godev/GoCart-Server/pkg/grpcclient"
)

// CreateStore is the resolver for the createStore field.
func (r *mutationResolver) CreateStore(ctx context.Context, input model.CreateStoreInput) (*model.Store, error) {
	if r.Clients == nil || r.Clients.StoreClient == nil {
		return nil, appErrors.Internal(nil, "store service client unavailable")
	}

	userCtx, _ := auth.UserFromContext(ctx)
	merchantID := ""
	if userCtx != nil {
		merchantID = userCtx.UserID
	}

	desc := ""
	if input.Description != nil {
		desc = *input.Description
	}

	logo := ""
	if input.LogoURL != nil {
		logo = *input.LogoURL
	}

	banner := ""
	if input.BannerURL != nil {
		banner = *input.BannerURL
	}

	addr := ""
	if input.Address != nil {
		addr = *input.Address
	}

	bank := maps.SerializeBankAccount(input.BankAccount, input.BankAccountDetails)

	res, err := r.Clients.StoreClient.CreateStore(ctx, &storepb.CreateStoreRequest{
		MerchantId:         merchantID,
		Name:               input.Name,
		Description:        desc,
		LogoUrl:            logo,
		BannerUrl:          banner,
		Address:            addr,
		BankAccountDetails: bank,
	})
	if err != nil {
		return nil, grpcclient.TranslateGRPCError(err)
	}

	return maps.MapStore(res.Store), nil
}

// UpdateStore is the resolver for the updateStore field.
func (r *mutationResolver) UpdateStore(ctx context.Context, input model.UpdateStoreInput) (*model.Store, error) {
	if r.Clients == nil || r.Clients.StoreClient == nil {
		return nil, appErrors.Internal(nil, "store service client unavailable")
	}

	userCtx, _ := auth.UserFromContext(ctx)
	merchantID := ""
	if userCtx != nil {
		merchantID = userCtx.UserID
	}

	targetID := ""
	if input.ID != nil {
		targetID = *input.ID
	}

	var bankDetails *string
	if input.BankAccount != nil || input.BankAccountDetails != nil {
		serialized := maps.SerializeBankAccount(input.BankAccount, input.BankAccountDetails)
		bankDetails = &serialized
	}

	res, err := r.Clients.StoreClient.UpdateStore(ctx, &storepb.UpdateStoreRequest{
		Id:                 targetID,
		MerchantId:         merchantID,
		Name:               input.Name,
		Description:        input.Description,
		LogoUrl:            input.LogoURL,
		BannerUrl:          input.BannerURL,
		Address:            input.Address,
		PublishStatus:      input.PublishStatus,
		BankAccountDetails: bankDetails,
	})
	if err != nil {
		return nil, grpcclient.TranslateGRPCError(err)
	}

	return maps.MapStore(res.Store), nil
}

// GenerateStoreUploadURL is the resolver for the generateStoreUploadUrl field.
func (r *mutationResolver) GenerateStoreUploadURL(ctx context.Context, input model.GenerateStoreUploadURLInput) (*model.StoreUploadURLPayload, error) {
	if r.Clients == nil || r.Clients.StoreClient == nil {
		return nil, appErrors.Internal(nil, "store service client unavailable")
	}

	userCtx, _ := auth.UserFromContext(ctx)
	merchantID := ""
	if userCtx != nil {
		merchantID = userCtx.UserID
	}

	contentType := ""
	if input.ContentType != nil {
		contentType = *input.ContentType
	}

	res, err := r.Clients.StoreClient.GetUploadUrl(ctx, &storepb.GetUploadUrlRequest{
		MerchantId:  merchantID,
		ImageType:   input.ImageType,
		Filename:    input.Filename,
		ContentType: contentType,
	})
	if err != nil {
		return nil, grpcclient.TranslateGRPCError(err)
	}

	return &model.StoreUploadURLPayload{
		UploadURL:        res.UploadUrl,
		PublicURL:        res.PublicUrl,
		Key:              res.Key,
		ExpiresInSeconds: int(res.ExpiresInSeconds),
	}, nil
}

// Store is the resolver for the store field.
func (r *queryResolver) Store(ctx context.Context, id string) (*model.Store, error) {
	if r.Clients == nil || r.Clients.StoreClient == nil {
		return nil, appErrors.Internal(nil, "store service client unavailable")
	}
	if id == "" {
		return nil, appErrors.BadRequest("store id is required")
	}

	res, err := r.Clients.StoreClient.GetStore(ctx, &storepb.GetStoreRequest{
		Id: id,
	})
	if err != nil {
		return nil, grpcclient.TranslateGRPCError(err)
	}

	return maps.MapStore(res.Store), nil
}

// MyStore is the resolver for the myStore field.
func (r *queryResolver) MyStore(ctx context.Context) (*model.Store, error) {
	if r.Clients == nil || r.Clients.StoreClient == nil {
		return nil, appErrors.Internal(nil, "store service client unavailable")
	}

	userCtx, _ := auth.UserFromContext(ctx)
	merchantID := ""
	if userCtx != nil {
		merchantID = userCtx.UserID
	}

	res, err := r.Clients.StoreClient.GetStore(ctx, &storepb.GetStoreRequest{
		MerchantId: merchantID,
	})
	if err != nil {
		return nil, grpcclient.TranslateGRPCError(err)
	}

	return maps.MapStore(res.Store), nil
}

// Stores is the resolver for the stores field.
func (r *queryResolver) Stores(ctx context.Context, merchantID *string, limit *int, offset *int) (*model.StoreList, error) {
	if r.Clients == nil || r.Clients.StoreClient == nil {
		return nil, appErrors.Internal(nil, "store service client unavailable")
	}

	mID := ""
	if merchantID != nil {
		mID = *merchantID
	}

	var l, o int32
	if limit != nil {
		l = int32(*limit)
	}
	if offset != nil {
		o = int32(*offset)
	}

	res, err := r.Clients.StoreClient.ListStores(ctx, &storepb.ListStoresRequest{
		MerchantId: mID,
		Limit:      l,
		Offset:     o,
	})
	if err != nil {
		return nil, grpcclient.TranslateGRPCError(err)
	}

	stores := make([]*model.Store, 0, len(res.Stores))
	for _, s := range res.Stores {
		stores = append(stores, maps.MapStore(s))
	}

	return &model.StoreList{
		Stores: stores,
		Total:  int(res.Total),
	}, nil
}
