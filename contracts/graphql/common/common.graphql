enum Role {
  ADMIN
  MERCHANT
  CUSTOMER
}

scalar Upload

directive @auth(requires: [Role!]) on FIELD_DEFINITION | OBJECT

type Query {
  health: String!
  version: String!
}

type Mutation {
  _empty: String
}

